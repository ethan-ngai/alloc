; OpenClaw's postinstall aborts on unsupported Node majors, which would fail the
; whole workspace install for contributors who never run the agent locally. No
; dependency here needs a build script.
ignore-scripts=true
audit=false
fund=false
